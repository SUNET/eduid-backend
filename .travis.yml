language: python
dist: xenial
python:
  - "3.7"
before_install:
  - docker pull docker.sunet.se/eduid/mongodb:latest
# Have Travis CI start mongodb for us
services:
  - docker
# command to install dependencies
install:
  - pip install -U setuptools
  - python setup.py testing
# command to run tests
script:
  - pytest
