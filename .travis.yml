sudo: false
language: python
python:
  - "2.7"
#  - "3.5"
# We need a mongo binary
services:
  - mongodb
# command to install dependencies
install:
  - pip install -U setuptools
  - python setup.py develop
  - pip install nosexcover WebTest==2.0.18
# command to run tests
script:
  - nosetests
