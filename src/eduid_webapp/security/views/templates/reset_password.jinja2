{% extends "base.jinja2" %}

{% block title %}{% trans %}Reset password - Email{% endtrans %}{% endblock %}

{% block content %}
    <div class="row">
        <h2>{{ view_context.heading }}</h2>
        {% if view_context.text %}
            <p><span class="glyphicon glyphicon-info-sign"></span> {{ view_context.text }}</p>
        {% endif %}
    </div>
    <div class="row">
        {% if not view_context.form_post_success %}
            <form class="form" method="post" action="{{ view_context.action }}">
                <input type="hidden" id="csrf" name="csrf" value="{{ view_context.csrf_token }}">
                <div class="form-group {% if view_context.errors %}has-error{% endif %}">
                    <div>
                        <input type="email" class="form-control" id="email" name="email" placeholder="{% trans %}Email address{% endtrans %}">
                    </div>
                    {% if view_context.errors %}
                        {% for key, value in view_context.errors.items() %}
                            <span id="helpblock" class="help-block">{{ value[0] }}</span>
                        {% endfor %}
                    {% endif %}
                </div>
                <div class="form-group">
                    <div>
                        <button type="submit" class="btn btn-default">{% trans %}Reset password{% endtrans %}</button>
                    </div>
                </div>
            </form>
        {% else %}
            <p>{{ view_context.form_post_success_msg }}</p>
        {% endif %}
    </div>
{% endblock %}